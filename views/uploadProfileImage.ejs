<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Profile Image</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .upload-container {
      background-color: #1e1e1e;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
      width: 90%;
      max-width: 400px;
      text-align: center;
    }

    .upload-container h2 {
      margin-bottom: 1rem;
    }

    input[type="file"] {
      margin: 1rem 0;
      background-color: #2c2c2c;
      color: #fff;
      border: none;
      padding: 0.5rem;
      border-radius: 5px;
      width: 100%;
    }

    button {
      background-color: #007bff;
      color: #fff;
      border: none;
      padding: 0.7rem 1.5rem;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="upload-container">
    <h2>Upload Profile Image</h2>
    <form action="/profile/image" method="POST" enctype="multipart/form-data">
      <label for="profile_image">Upload Profile Image:</label>
      <input type="file" name="profile_image" required />
      <button type="submit">Upload</button>
    </form>
    
  </div>
</body>
</html> -->


<!-- Try 2 -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Profile Image</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .upload-container {
      background-color: #1e1e1e;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
      width: 90%;
      max-width: 400px;
      text-align: center;
    }

    .upload-container h2 {
      margin-bottom: 1rem;
    }

    input[type="file"] {
      margin: 1rem 0;
      background-color: #2c2c2c;
      color: #fff;
      border: none;
      padding: 0.5rem;
      border-radius: 5px;
      width: 100%;
    }

    button {
      background-color: #007bff;
      color: #fff;
      border: none;
      padding: 0.7rem 1.5rem;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    .profile-preview {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 1rem;
      border: 2px solid #333;
    }
  </style>
</head>
<body>
  <div class="upload-container">
    <h2>Upload Profile Image</h2>

    <!-- Display current profile image -->
    <img class="profile-preview" src="/uploads/<%= user.profile_image %>" alt="Profile Image">

    <form action="/profile/image" method="POST" enctype="multipart/form-data">
      <label for="profile_image">Upload New Profile Image:</label>
      <input type="file" name="profile_image" required />
      <button type="submit">Upload</button>
    </form>
  </div>
</body>
</html>


<!-- End Try 2 -->